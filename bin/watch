#!/usr/bin/env sh

if ! type "$watchman" &> /dev/null; then
  watchman watch $PWD
  watchman -- trigger $PWD/less lessfiles '*.less' -- npm run-script prestart
  watchman -- trigger $PWD/js jsfiles '*.js' -- npm run-script prestart
fi
